<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        body {
            font-family: 'Roboto', Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }
        .email-container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #ffffff;
            border: none;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        .header {
            background-color: rgb(27, 57, 48);
            padding: 30px;
            text-align: center;
        }
        .header img {
            max-width: 180px;
        }
        .content {
            padding: 30px;
            color: #333;
            line-height: 1.8;
        }
        .content h1 {
            font-size: 24px;
            margin-bottom: 20px;
            color: rgb(27, 57, 48);
        }
        .content p {
            font-size: 16px;
            margin-bottom: 15px;
        }
        .otp {
            font-size: 36px;
            font-weight: bold;
            color: rgb(27, 57, 48); 
            text-align: center;
            margin: 20px 0;
        }
        .button {
            display: inline-block;
            padding: 12px 25px;
            margin: 20px 0;
            background-color: rgb(27, 57, 48);
            color: #ffffff;
            text-decoration: none;
            border-radius: 50px;
            font-size: 16px;
            transition: background-color 0.3s;
        }
        .button:hover {
            background-color: rgb(34, 70, 58);
        }
        .footer {
            background-color: #f0f0f0;
            padding: 20px;
            text-align: center;
            font-size: 14px;
            color: #555;
            border-top: 1px solid #ddd;
        }
        .footer a {
            color: rgb(27, 57, 48);
            text-decoration: none;
        }
        .footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="header">
            <img src="{{logoUrl}}" alt="Škoda Logo">
        </div>
        <div class='content'>
        {{!-- <h1>{{headerText}}</h1> --}}
        <p>Dear {{recipientName}},</p>
        {{> (lookup . 'partialTemplateName') }}
        <p>Best regards, <br>
        <strong>{{senderName}}</strong>
        </p>
        <div class="footer">
            &copy; {{currentYear}} {{companyName}}. All Rights Reserved. <br>
            <a href="{{privacyPolicyUrl}}">Privacy Policy</a> | <a href="{{termsOfServiceUrl}}">Terms of Service</a>
        </div>
    </div>
</body>
</html>

